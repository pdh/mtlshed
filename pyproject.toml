[project]
name = "wtph"
description = "what the ph? dumb mtls cert tool"
version = "0.0.1"

[project.scripts]
wtph-certs = "wtph.certs:main"

[build-system]
build-backend = 'setuptools.build_meta'
requires = [
    'setuptools', 'wheel'
]

